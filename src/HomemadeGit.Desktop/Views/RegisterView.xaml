<UserControl x:Class="HomemadeGit.Desktop.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      
             mc:Ignorable="d" 
             xmlns:views="clr-namespace:HomemadeGit.Desktop.Views"
             xmlns:vm="clr-namespace:HomemadeGit.Desktop.ViewModel"
             
             d:DataContext="{d:DesignInstance Type=vm:RegisterViewModel}"
             
              d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate x:Key="SignInTemplate">
            <views:SignIn />
        </DataTemplate>

        <DataTemplate x:Key="SignUpTemplate">
            <views:SignUp />
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <ContentControl Content="{Binding}">
            <ContentControl.Style>
                <Style TargetType="ContentControl">
                    <Setter Property="ContentTemplate" Value="{StaticResource SignInTemplate}" />

                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsCheck}" Value="True">
                            <Setter Property="ContentTemplate" Value="{StaticResource SignUpTemplate}" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ContentControl.Style>
        </ContentControl>
    </Grid>
    
</UserControl>
